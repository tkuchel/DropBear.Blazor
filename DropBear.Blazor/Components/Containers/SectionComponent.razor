@* /Components/SectionComponent.razor *@
@if (Predicate == null || Predicate.Invoke())
{
    <div class="section-component-section">
        @ChildContent
    </div>
}

@code {
    // Intentionally left blank
}
